const PASSWORD_LENGTH = {
  MIN: 8,
  MAX: 64,
};

const USER_NAME_LENGTH = {
  MIN: 2,
  MAX: 32,
};

const EMAIL_LENGTH = {
  MAX: 64,
};

module.exports = {
  PASSWORD_LENGTH,
  USER_NAME_LENGTH,
  EMAIL_LENGTH,
};
